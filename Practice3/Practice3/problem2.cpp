/*
#include <iostream>
using namespace std;

int add(int x, int y)
{
	return x + y;
}

int sub(int x, int y)
{
	return x - y;
}

int mul(int x, int y)
{
	return x * y;
}


int div_q(int x, int y)
{
	return x / y;
}

int div_r(int x, int y)
{
	return x % y;
}

void calculate(int x, int y)
{
	cout << "Addition : " << add(x, y) << endl;
	cout << "Subtraction : " << sub(x, y) << endl;
	cout << "Multiplication : " << mul(x, y) << endl;
	cout << "Division_quotient : " << div_q(x, y) << endl;
	cout << "Division_remainder: " << div_r(x, y) << endl;
}

int main()
{
	int x, y;
	cin >> x >> y;
	calculate(x, y);
	return 0;
}

*/